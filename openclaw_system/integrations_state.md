# Integrations State

## Фактический baseline на момент Этапа 7
- `n8n` не найден в PATH
- `docker` доступен: `/usr/bin/docker`
- `python3` доступен: `/usr/bin/python3`
- каталог `/root/.n8n` не подтверждён этой проверкой
- каталог `/root/.openclaw/workspace/n8n` не подтверждён этой проверкой

## Практический вывод
- отдельный рабочий слой интеграций можно зафиксировать архитектурно
- но n8n пока не подтверждён как установленный и активный runtime-компонент
- значит текущий integrations layer — это описанный системный слой с честно зафиксированным baseline
